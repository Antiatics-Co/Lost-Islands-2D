function init(self)
	msg.post(".", "acquire_input_focus")
	gui.set_enable(gui.get_node("interface"), false)
end

function on_input(self, action_id, action)
	if action_id == hash("esc") and action.released then
		gui.set_enable(gui.get_node("interface"), true)
	end
end

