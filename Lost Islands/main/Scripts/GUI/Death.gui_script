function init(self)
	local id = go.get_id("/main/main.collection")
end

function on_message(self, message_id, message, sender)
	if message_id == hash("set_enabled") then 
		gui.set_enabled("box", true)
	end
end
